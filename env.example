# ============================================
# CREDIT Inventory Dashboard - Environment Variables
# ============================================
# Copy this file to .env and update with your values
# cp env.example .env

# ============================================
# Frontend Configuration
# ============================================
# Backend API URL for frontend to connect to
# In Docker, use: http://localhost:8000 (or your server's IP)
# For production, use your domain: https://api.yourdomain.com
VITE_API_URL=http://localhost:8000

# ============================================
# Backend Configuration
# ============================================
# Frontend URL for QR code generation
# This should be the public URL where your frontend is accessible
# In Docker, use: http://localhost (or your server's IP)
# For production, use your domain: https://yourdomain.com
FRONTEND_URL=http://localhost

# API Port (default: 8000)
API_PORT=8000

# Frontend Port (default: 80 in production)
FRONTEND_PORT=80

# ============================================
# Email Configuration (Optional)
# ============================================
# These are optional - only needed if you want email notifications
# Leave empty if you don't need email functionality

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=CREDIT Inventory System
SMTP_USE_TLS=true

# ============================================
# Database Configuration
# ============================================
# SQLite database is stored in ./backend/inventory.db
# This is automatically mounted as a volume in Docker
# No configuration needed unless you want to use a different database

# ============================================
# Development vs Production
# ============================================
# For local development:
#   VITE_API_URL=http://localhost:8000
#   FRONTEND_URL=http://localhost:5173
#
# For Docker/production:
#   VITE_API_URL=http://localhost:8000
#   FRONTEND_URL=http://localhost
#
# For production with domain:
#   VITE_API_URL=https://api.yourdomain.com
#   FRONTEND_URL=https://yourdomain.com


